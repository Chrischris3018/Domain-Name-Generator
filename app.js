let pronoun = ['the','our'];
let adj = ['great', 'big' ];
let noun = ['jogger','racoon'];

const type1 = function(){
    for (const element of array1) {
        console.log(element);
    }
}

for (const element of array1) {
    console.log(element);
}

const array1 = [];

// return `${pronoun1}${adj2}${noun3}.com`;
console.log(type1())